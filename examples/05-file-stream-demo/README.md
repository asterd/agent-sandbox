# 05 File Stream Demo

This example exercises the Scenario 1 workflow:

- upload a file into the sandbox
- execute a command with streamed NDJSON events
- download the generated artifact
- destroy the sandbox through the normal SDK lifecycle

Run:

```bash
python3 run.py
```
